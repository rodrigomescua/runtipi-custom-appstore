version: '3'

services:
  razor-pricehistory:
    image: romesc/razorpricehistory:1743737648
    environment:
      - TZ=${TZ}
    volumes:
      - ${APP_DATA_DIR}/data/db:/app/db
    x-runtipi:
      is_main: true
      internal_port: 8080

x-runtipi:
  schema_version: 2
