version: '3'

services:
  rustpnews:
    image: ghcr.io/rodrigomescua/rustpnews:202605281307
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/data/App_Data:/app/App_Data
    environment:
      - RUSTPNEWS_API_TOKEN=${RUSTPNEWS_API_TOKEN}
      - RUSTPNEWS_POLL_INTERVAL_SECONDS=120
      - RUSTPNEWS_PAGE_SIZE=25
      - RUSTPNEWS_DETAIL_CONCURRENCY=4
      - RUSTPNEWS_MAX_PAGES=10
      - RUSTPNEWS_FROM_OVERLAP_MINUTES=5
      - RUSTPNEWS_CHANGES_RETRY_ATTEMPTS=3
      - RUSTPNEWS_CHANGES_RETRY_BASE_DELAY_MS=500
    x-runtipi:
      is_main: true
      internal_port: 8080

x-runtipi:
  schema_version: 2
