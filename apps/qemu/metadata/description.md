# QEMU

Run a Linux virtual machine inside Docker with QEMU and KVM acceleration. Select an operating system, allocate CPU, RAM and disk space, then access the VM through the browser-based viewer.

This app requires a host with KVM support and exposes the viewer on port 8006 inside the container.
